<%- include('../partials/mainheader.ejs')%>
<style>
    /* Apply sans-serif font family to the entire form area */
    .login-register-area {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    
    /* Ensure all form elements use sans-serif */
    .login-register-area * {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    #name::placeholder {
        color: #999;
        font-style: italic;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    #email::placeholder {
        color: #999;
        font-style: italic;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    #password::placeholder {
        color: #999;
        font-style: italic;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    #mno::placeholder {
        color: #999;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        /* font-style: italic; */
    }
    #referdid::placeholder {
        color: #999;
        font-style: italic;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    /* Message styling */
    .message-text {
        color: #E97730;
        text-align: center;
        font-weight: 500;
        margin-bottom: 15px;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .error-message {
        color: #E97730;
        font-size: 14px;
        margin-top: 5px;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    /* Form elements styling */
    .login-register-form label {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .login-register-form input {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .login-register-form button {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .login-register-tab-list h4 {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    #show1{
        color: red;
    }
</style>
      
<div class="login-register-area" style="padding-top: 30px;">
    <div class="container">
        <div class="row">
            <div class="col-lg-8 col-md-12 offset-lg-2">
                <div class="login-register-wrapper">
                    <div class="login-register-tab-list nav">
                        <a href="/auth/login/">
                            <h4> login </h4>
                        </a>
                        <a class="active" href="#lg2">
                            <h4> register </h4>
                        </a>
                    </div>
                    <div class="tab-content">
                        <div id="lg1" class="tab-pane active">
                            <div class="login-form-container">
                                <div class="login-register-form">

                                    <p id="show1" class="message-text"></p>

                                    <% if(typeof message !=='undefined' ){ %>
                                        <p class="message-text">
                                            <%= message %>
                                        </p>
                                    <% } %>
                                    
                                    <form action="" onsubmit="return handleSubmit()" method="post" novalidate>
                                        <label>Enter your Name :</label>
                                        <input type="text" id="name" name="name" placeholder="your name" required>
                                        
                                        <label>Enter your Email :</label>
                                        <input type="email" id="email" name="email" placeholder="(e.g., example@example.com)" required>
                                        
                                        <label>Enter your Mobile/phone No :</label>
                                        <input type="number" id="mno" name="mno" placeholder="10 Digit mobile no" required>
                                        
                                        <label>Enter your Password :</label>
                                        <input type="password" id="password" name="password" placeholder="Enter your password" required>
                                        
                                        <label>Referral Id :</label>
                                        <input type="text" id="referdid" name="referdid" placeholder="Optional">
                                        
                                        <div class="button-box btn-hover" style="justify-content: center; align-items: center; display: flex;">
                                            <button type="submit">Register</button>
                                        </div>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<br>
<br>

<script>
    let show = document.getElementById("show1");

    function handleSubmit() {
    // Clear previous error messages
    show.innerHTML = "";
    
    let username = document.getElementById("name").value;
    let email = document.getElementById("email").value;
    let mobile = document.getElementById("mno").value;
    let password = document.getElementById("password").value;
    let referralId = document.getElementById("referdid").value;

    // Name validation - cannot be empty and should contain only letters and spaces
    let nameRegex = /^[a-zA-Z\s]+$/;
    if (username.trim() === "") {
        show.innerHTML = "Name cannot be empty";
        return false;
    }
    if (!username.match(nameRegex)) {
        show.innerHTML = "Name should contain only letters and spaces";
        return false;
    }

    // Email validation
    let emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!email.match(emailRegex)) {
        show.innerHTML = "Please enter a valid email address";
        return false;
    }

    // Mobile validation - exactly 10 digits
    let mobileRegex = /^\d{10}$/;
    if (!mobile.match(mobileRegex)) {
        show.innerHTML = "Mobile number must be exactly 10 digits";
        return false;
    }

    // Password validation - minimum 6 characters, at least one uppercase, one digit, one symbol
    if (password.length < 6) {
        show.innerHTML = "Password must be at least 6 characters long";
        return false;
    }
    
    let hasUppercase = /[A-Z]/.test(password);
    let hasDigit = /\d/.test(password);
    let hasSymbol = /[!@#$%^&*()_+\-=\[\]{};':"\\|,.<>\/?~`]/.test(password);
    
    if (!hasUppercase) {
        show.innerHTML = "Password must contain at least one uppercase letter";
        return false;
    }
    
    if (!hasDigit) {
        show.innerHTML = "Password must contain at least one digit";
        return false;
    }
    
    if (!hasSymbol) {
        show.innerHTML = "Password must contain at least one symbol (!@#$%^&*()_+-=[]{}|;':\",./<>?~`)";
        return false;
    }

    // Referral ID is optional - no validation needed
    // If referralId is empty, it's okay

    return true;
}
</script>

<%- include('../partials/mainfooter.ejs')%>